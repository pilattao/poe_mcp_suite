# Reproducible PoE2 suite runtime. Node dependencies live in pob-mcp/package-lock.json.
# Install with: python -m pip install -r requirements.txt
-r poe-trade-mcp/requirements.txt
-e ./poe-data-mcp
# Native Lua boundary verification uses the same runtime as the data reader.
lupa>=2.6,<3
